<script>
    $(document).ready(function () {
        $('#dt-function').DataTable({
            'paging': true,
            'lengthChange': true,
            'searching': true,
            'ordering': true,
            'info': true,
            'autoWidth': true
        });
    });
</script>

<div class="card">
    {{!-- <div class="card-header bg-transparent">
        <h3 class="mb-0">Icons</h3>
    </div> --}}
    <div class="card-body">
        {{!-- <div class="table-responsive"> --}}
            <table id="dt-function" class="table align-items-center table-flush" style="width:100%">
                <thead>
                    <tr>
                        <th>No.</th>
                        <th>Nama</th>
                        <th>Desc</th>
                        <th>Div</th>
                        <th>isGroup</th>
                        <th>isDTO</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each allGroups}}
                    <tr>
                        <td>{{dataValues.id}}</td>
                        <td>{{dataValues.group_abbr}}</td>
                        <td>{{dataValues.group_full}}</td>
                        <td>{{dataValues.group_div}}</td>
                        <td>{{dataValues.group_is_group}}</td>
                        <td>{{dataValues.group_is_dto}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</div>